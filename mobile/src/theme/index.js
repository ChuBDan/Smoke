import colors from "./colors";
import {
  spacing,
  borderRadius,
  fontSize,
  fontWeight,
  shadows,
  screen,
} from "./spacing";

export const theme = {
  colors,
  spacing,
  borderRadius,
  fontSize,
  fontWeight,
  shadows,
  screen,
};

export default theme;
